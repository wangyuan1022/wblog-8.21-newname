# Command to run when "Run" button clicked
run_command: bin/dev
# Command to install or update dependencies, will execute each time a new thread created to ensure dependencies up-to-date
dependency_command: gem install bundler && bundle install && yarn install && cp config/application.yml.example config/application.yml && cp config/database.yml.example config/database.yml && bin/rails db:setup
